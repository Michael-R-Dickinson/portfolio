import ImageSection from "../image_section"
import InfoSection from "./info_section"

function Hero({ }) {
    return (
        <div className="hero">
            <InfoSection />
            <ImageSection />
        </div>

    )
}

export default Hero